import React from 'react';

interface MultimodalStatusBarProps {
  className?: string;
  showDetails?: boolean;
}

export const MultimodalStatusBar: React.FC<MultimodalStatusBarProps> = () => null;

export const MultimodalStatusPanel: React.FC = () => null;
